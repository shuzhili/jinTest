1.javah  encoding的问题
    javah -jni -encoding UTF-8

2.解决c++ - Android NDK: Missing LOCAL_MODULE before including BUILD_SHARED_LIBRARY
    module写错了

3.NDK中配置好了c++11标准以及STL，仍然无法使用STL里的函数
    在头文件中添加 using namespace std;

4. Android NDK: APP_STL stlport_static is no longer supported.
 Please switch to either c++_static or c++_shared.
    APP_STL := stlport_static -> APP_STL := c++_static (c++_shared)

5. 在使用ndk-r9版本的ndk-build命令编译jni时，
 报错error: format not a string literal and no format arguments [-Werror=format-security]
    Application.mk 中添加 APP_CFLAGS += -Wno-error=format-security

6.集成地图后白屏
    把armeabi -> armeabi-v7a

 7.写好OBOJNI.java中native方法，进入E:\MY_PRO\JNI目录 javah -jni -encoding UTF-8 com.example.myapplication.OBOJNI

    得到jni接口，编写cpp文件。配置Android.mk(LOCAL_SRC_FILES=xxx.cpp) Application.mk，进入E:\MY_PRO\JNI
    执行ndk-build
